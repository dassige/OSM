dbs:
  - path: /app/fenz.db
    replicas:
      # This will work for S3 by setting REPLICA_URL=s3://your-s3-bucket/fenz.db
      # Or for GCS by setting REPLICA_URL=gcs://${GCS_BUCKET_NAME}/fenz.db
      - url: ${REPLICA_URL}