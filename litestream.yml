dbs:
  - path: /app/fenz.db
    replicas:
      - url: gcs://${GCS_BUCKET_NAME}/fenz.db
    restore:
      if-not-exists: true

  - path: /app/sessions.db
    replicas:
      - url: gcs://${GCS_BUCKET_NAME}/sessions.db
    restore:
      if-not-exists: true
