{
  "permissions": {
    "allow": [
      "Bash(node -e ' *)",
      "Bash(Get-ChildItem -Path . -Recurse -Depth 2)",
      "Bash(Select-Object -First 50)",
      "Bash(Format-Table -Property Name, FullName)",
      "Bash(awk '{printf \"Logo.png: %d bytes = %.1f KB\\\\n\", $1, $1/1024}')"
    ]
  }
}
